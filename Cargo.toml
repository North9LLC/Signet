[package]
name = "signet-fsk-proto"
version = "0.0.1"
edition = "2021"

[dependencies]
sha2 = "0.10"
rand = "0.8"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[profile.release]
opt-level = 3
lto = "thin"
